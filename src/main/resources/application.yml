server:
  port: 8080

spring:
  datasource:
    url: jdbc:sqlite:./data/updatesteps.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    show-sql: false
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

app:
  jwt:
    secret: "UpdateStepsSecretKey2024VeryLongSecretKeyForJWTSigningNeedToBeAtLeast256Bits"
    expiration: 86400000
  upload:
    path: ./uploads
  admin:
    username: admin
    password: "nbcb,111"
